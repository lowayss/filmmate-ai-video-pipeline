from pathlib import Path


def replace_once(path, old, new):
    file = Path(path)
    text = file.read_text(encoding="utf-8")
    count = text.count(old)
    if count != 1:
        raise SystemExit(f"{path}: expected one match, found {count}: {old[:90]!r}")
    file.write_text(text.replace(old, new, 1), encoding="utf-8")


# Production Agent durable queue hardening.
replace_once(
    "core/production_agent_jobs.py",
    '    active = {row["signature"]: row for row in existing if row["state"] not in {"COMPLETE", "FAILED"}}\n',
    '    active = {row["signature"]: row for row in existing if row["state"] != "COMPLETE"}\n',
)
replace_once(
    "core/production_agent_jobs.py",
    '        state = row["state"] if row["state"] == "CLAIMED" else base_state\n',
    '        state = row["state"] if row["state"] in {"CLAIMED", "FAILED"} else base_state\n',
)
replace_once(
    "core/production_agent_jobs.py",
    '''        state = _run_state(plan, paused=bool(run["paused"]), cancelled=bool(run["cancelled"]))\n        now = hap_core.now()\n''',
    '''        state = _run_state(plan, paused=bool(run["paused"]), cancelled=bool(run["cancelled"]))\n        failed = db.execute("SELECT 1 FROM production_agent_tasks WHERE run_id=? AND state='FAILED' LIMIT 1", (run_id,)).fetchone()\n        if failed is not None and not run["paused"] and not run["cancelled"]:\n            state = "FAILED"\n        now = hap_core.now()\n''',
)
replace_once(
    "core/production_agent_jobs.py",
    '''        task = db.execute(\n            "SELECT * FROM production_agent_tasks WHERE run_id=? AND state='PENDING' ORDER BY ordinal,created_at LIMIT 1",\n            (run_id,),\n        ).fetchone()\n        if task is None:\n            raise ValueError("E_PRODUCTION_AGENT_NO_CLAIMABLE_TASK")\n        if not task["suggested_tool"]:\n            raise ValueError("E_PRODUCTION_AGENT_TASK_REQUIRES_MANUAL_ACTION")\n        token = hap_core.new_id("agent_claim", f"{run_id}|{task['task_id']}|{actor}")\n        now = hap_core.now()\n        db.execute(\n            "UPDATE production_agent_tasks SET state='CLAIMED',claim_actor=?,claim_token=?,claim_checkpoint=?,claimed_at=?,updated_at=? WHERE task_id=? AND state='PENDING'",\n            (actor, token, run["checkpoint"], now, now, task["task_id"]),\n        )\n        if db.total_changes == 0:\n            raise ValueError("E_PRODUCTION_AGENT_TASK_CLAIM_RACE")\n''',
    '''        task = db.execute(\n            "SELECT * FROM production_agent_tasks WHERE run_id=? AND state NOT IN ('COMPLETE','FAILED') ORDER BY ordinal,created_at LIMIT 1",\n            (run_id,),\n        ).fetchone()\n        if task is None:\n            raise ValueError("E_PRODUCTION_AGENT_NO_CLAIMABLE_TASK")\n        if task["state"] == "CLAIMED":\n            raise ValueError("E_PRODUCTION_AGENT_TASK_ALREADY_CLAIMED")\n        if task["state"] != "PENDING":\n            raise ValueError(f"E_PRODUCTION_AGENT_TASK_NOT_CLAIMABLE:{task['state']}")\n        if not task["suggested_tool"]:\n            raise ValueError("E_PRODUCTION_AGENT_TASK_REQUIRES_MANUAL_ACTION")\n        token = hap_core.new_id("agent_claim", f"{run_id}|{task['task_id']}|{actor}")\n        now = hap_core.now()\n        cursor = db.execute(\n            "UPDATE production_agent_tasks SET state='CLAIMED',claim_actor=?,claim_token=?,claim_checkpoint=?,claimed_at=?,updated_at=? WHERE task_id=? AND state='PENDING'",\n            (actor, token, run["checkpoint"], now, now, task["task_id"]),\n        )\n        if cursor.rowcount != 1:\n            raise ValueError("E_PRODUCTION_AGENT_TASK_CLAIM_RACE")\n''',
)
replace_once(
    "core/production_agent_jobs.py",
    '''        elif action == "fail_task":\n            if not task_id or not claim_token:\n                raise ValueError("E_PRODUCTION_AGENT_TASK_CLAIM_REQUIRED")\n            task = _task_row(db, run_id, task_id)\n            if task["state"] != "CLAIMED" or task["claim_token"] != claim_token:\n                raise ValueError("E_PRODUCTION_AGENT_TASK_CLAIM_INVALID")\n            db.execute("UPDATE production_agent_tasks SET state='FAILED',last_error=?,updated_at=? WHERE task_id=?", (error or "task_failed", now, task_id))\n            db.execute("UPDATE production_agent_runs SET state='FAILED',last_error=?,updated_at=? WHERE run_id=?", (error or "task_failed", now, run_id))\n            _event(db, run_id, "task_failed", actor, {"error": error or "task_failed"}, task_id)\n        else:\n''',
    '''        elif action == "fail_task":\n            if not task_id or not claim_token:\n                raise ValueError("E_PRODUCTION_AGENT_TASK_CLAIM_REQUIRED")\n            task = _task_row(db, run_id, task_id)\n            if task["state"] != "CLAIMED" or task["claim_token"] != claim_token:\n                raise ValueError("E_PRODUCTION_AGENT_TASK_CLAIM_INVALID")\n            db.execute("UPDATE production_agent_tasks SET state='FAILED',last_error=?,updated_at=? WHERE task_id=?", (error or "task_failed", now, task_id))\n            db.execute("UPDATE production_agent_runs SET state='FAILED',last_error=?,updated_at=? WHERE run_id=?", (error or "task_failed", now, run_id))\n            _event(db, run_id, "task_failed", actor, {"error": error or "task_failed"}, task_id)\n        elif action == "retry_task":\n            if not task_id:\n                raise ValueError("E_PRODUCTION_AGENT_TASK_REQUIRED")\n            task = _task_row(db, run_id, task_id)\n            if task["state"] != "FAILED":\n                raise ValueError("E_PRODUCTION_AGENT_TASK_NOT_FAILED")\n            base = _base_task_state({"status": task["plan_status"]})\n            db.execute(\n                "UPDATE production_agent_tasks SET state=?,claim_actor=NULL,claim_token=NULL,claim_checkpoint=NULL,last_error=NULL,updated_at=? WHERE task_id=?",\n                (base, now, task_id),\n            )\n            db.execute("UPDATE production_agent_runs SET state='READY',last_error=NULL,updated_at=? WHERE run_id=?", (now, run_id))\n            _event(db, run_id, "task_retry_requested", actor, {}, task_id)\n        else:\n''',
)

# Bridge should find the latest run using any valid scene alias.
replace_once(
    "core/production_agent_bridge.py",
    '''    if action == "latest_run":\n        latest = production_agent_jobs.latest_run(root, str(aliases[0]))\n        if latest is None:\n            return None\n        return production_agent_jobs.refresh_run(root, latest["run_id"], projection, aliases, actor=str(payload.get("actor") or "filmmate-user"))\n''',
    '''    if action == "latest_run":\n        latest = None\n        for alias in aliases:\n            latest = production_agent_jobs.latest_run(root, str(alias))\n            if latest is not None:\n                break\n        if latest is None:\n            return None\n        return production_agent_jobs.refresh_run(root, latest["run_id"], projection, aliases, actor=str(payload.get("actor") or "filmmate-user"))\n''',
)

# MCP semantic queue tools.
replace_once(
    "mcp_server.py",
    "from core import filmmate_documents, hap_core, production_commands, production_orchestrator, prompt_ir, prompt_jobs\n",
    "from core import filmmate_documents, hap_core, production_agent_jobs, production_commands, production_orchestrator, prompt_ir, prompt_jobs\n",
)
replace_once(
    "mcp_server.py",
    '''        if name == "prepare_scene":\n''',
    '''        if name == "start_production_run":\n            root, projection, aliases = semantic_scene_context(args["project"], args["scene"])\n            return result(production_agent_jobs.start_run(\n                root, projection, aliases, goal=args.get("goal"), target=args.get("target"), actor=str(args.get("actor") or "codex")\n            ))\n        if name == "get_production_run":\n            root, projection, aliases = semantic_scene_context(args["project"], args["scene"])\n            return result(production_agent_jobs.refresh_run(\n                root, args["run_id"], projection, aliases, actor=str(args.get("actor") or "codex")\n            ))\n        if name == "claim_production_task":\n            root, projection, aliases = semantic_scene_context(args["project"], args["scene"])\n            return result(production_agent_jobs.claim_next(\n                root, args["run_id"], projection, aliases, actor=str(args.get("actor") or "codex-worker")\n            ))\n        if name == "control_production_run":\n            root, projection, aliases = semantic_scene_context(args["project"], args["scene"])\n            snapshot = production_agent_jobs.control_run(\n                root, args["run_id"], args["action"], actor=str(args.get("actor") or "codex"),\n                task_id=args.get("task_id"), claim_token=args.get("claim_token"), error=args.get("error"),\n            )\n            if args["action"] in {"resume", "retry_task"} and not snapshot.get("cancelled"):\n                snapshot = production_agent_jobs.refresh_run(root, args["run_id"], projection, aliases, actor=str(args.get("actor") or "codex"))\n            return result(snapshot)\n        if name == "prepare_scene":\n''',
)
replace_once(
    "mcp_server.py",
    '''    {\n        "name": "prepare_scene",\n''',
    '''    {\n        "name": "start_production_run",\n        "description": "Create a durable Production Agent run from the current canonical scene plan. The queue persists in HAP SQLite and completion remains derived from canonical state.",\n        "inputSchema": {"type": "object", "properties": {"project": {"type": "string"}, "scene": {"type": "string"}, "goal": {"type": "string"}, "target": {"type": "string", "enum": ["generate_ready", "handoff_ready", "stale_clear"]}, "actor": {"type": "string"}}, "required": ["project", "scene", "goal"]},\n    },\n    {\n        "name": "get_production_run",\n        "description": "Refresh a durable Production Agent run from current HAP state. Tasks disappear only when the canonical blocker is actually resolved.",\n        "inputSchema": {"type": "object", "properties": {"project": {"type": "string"}, "scene": {"type": "string"}, "run_id": {"type": "string"}, "actor": {"type": "string"}}, "required": ["project", "scene", "run_id"]},\n    },\n    {\n        "name": "claim_production_task",\n        "description": "Claim exactly the first executable Production Agent task for one worker. Later tasks cannot be claimed before the leading task resolves in canonical state.",\n        "inputSchema": {"type": "object", "properties": {"project": {"type": "string"}, "scene": {"type": "string"}, "run_id": {"type": "string"}, "actor": {"type": "string"}}, "required": ["project", "scene", "run_id"]},\n    },\n    {\n        "name": "control_production_run",\n        "description": "Pause, resume, cancel, release, fail, or explicitly retry a Production Agent task. It never marks creative work complete; canonical refresh decides that.",\n        "inputSchema": {"type": "object", "properties": {"project": {"type": "string"}, "scene": {"type": "string"}, "run_id": {"type": "string"}, "action": {"type": "string", "enum": ["pause", "resume", "cancel", "release_task", "fail_task", "retry_task"]}, "task_id": {"type": "string"}, "claim_token": {"type": "string"}, "error": {"type": "string"}, "actor": {"type": "string"}}, "required": ["project", "scene", "run_id", "action"]},\n    },\n    {\n        "name": "prepare_scene",\n''',
)

# Electron bridge: accept both planning and durable runtime responses.
replace_once(
    "desktop/python-bridge.cjs",
    '''    if (!parsed || parsed.ok !== true || !parsed.plan) {\n      throw new Error(parsed?.error || "production_agent_bridge_failed");\n    }\n    return parsed.plan;\n''',
    '''    if (!parsed || parsed.ok !== true || !Object.prototype.hasOwnProperty.call(parsed, "result")) {\n      throw new Error(parsed?.error || "production_agent_bridge_failed");\n    }\n    return parsed.result;\n''',
)

# Electron IPC: one validated payload builder, separate renderer actions.
replace_once(
    "desktop/main.cjs",
    '''ipcMain.handle("production-agent:run", async (_event, project, scene, request = {}) => {\n  const projectDir = projectPaths.resolveHapProject(project);\n  const {sceneDir} = projectPaths.resolveScene(project, scene);\n  const manifest = readJson(path.join(sceneDir, "scene-data", "scene-manifest.json")) || {};\n  const aliases = [scene, sceneDir.name, manifest.scene_id].filter(Boolean);\n  return await pythonBridge.runProductionAgentAsync({\n    project_root: projectDir,\n    scene_aliases: aliases,\n    goal: request?.goal,\n    target: request?.target,\n    previous_checkpoint: request?.previous_checkpoint,\n  });\n});\n''',
    '''function productionAgentPayload(project, scene, request = {}) {\n  const projectDir = projectPaths.resolveHapProject(project);\n  const {sceneDir} = projectPaths.resolveScene(project, scene);\n  const manifest = readJson(path.join(sceneDir, "scene-data", "scene-manifest.json")) || {};\n  return {\n    project_root: projectDir,\n    scene_aliases: [scene, sceneDir.name, manifest.scene_id].filter(Boolean),\n    ...request,\n  };\n}\n\nipcMain.handle("production-agent:run", async (_event, project, scene, request = {}) => {\n  return await pythonBridge.runProductionAgentAsync(productionAgentPayload(project, scene, {...request, action:"plan"}));\n});\nipcMain.handle("production-agent:start-run", async (_event, project, scene, request = {}) => {\n  return await pythonBridge.runProductionAgentAsync(productionAgentPayload(project, scene, {...request, action:"start_run", actor:"filmmate-user"}));\n});\nipcMain.handle("production-agent:get-run", async (_event, project, scene, runId) => {\n  const request = runId ? {action:"get_run", run_id:runId, actor:"filmmate-user"} : {action:"latest_run", actor:"filmmate-user"};\n  return await pythonBridge.runProductionAgentAsync(productionAgentPayload(project, scene, request));\n});\nipcMain.handle("production-agent:control-run", async (_event, project, scene, request = {}) => {\n  return await pythonBridge.runProductionAgentAsync(productionAgentPayload(project, scene, {...request, action:"control_run", actor:"filmmate-user"}));\n});\n''',
)
replace_once(
    "desktop/preload.cjs",
    '''  runProductionAgent: (project, scene, request) => ipcRenderer.invoke("production-agent:run", project, scene, request),\n''',
    '''  runProductionAgent: (project, scene, request) => ipcRenderer.invoke("production-agent:run", project, scene, request),\n  startProductionRun: (project, scene, request) => ipcRenderer.invoke("production-agent:start-run", project, scene, request),\n  getProductionRun: (project, scene, runId) => ipcRenderer.invoke("production-agent:get-run", project, scene, runId),\n  controlProductionRun: (project, scene, request) => ipcRenderer.invoke("production-agent:control-run", project, scene, request),\n''',
)

# Desktop queue UX.
replace_once(
    "desktop/index.html",
    '.agent-error{margin-top:10px;color:var(--danger);font-size:12px}.agent-checkpoint{font:10px ui-monospace,monospace;color:var(--muted)}@media(max-width:700px){.agent-command-row{grid-template-columns:1fr}.agent-result-head{display:block}.agent-step{grid-template-columns:24px 1fr}.agent-step span:last-child{grid-column:2}}\n',
    '.agent-error{margin-top:10px;color:var(--danger);font-size:12px}.agent-checkpoint{font:10px ui-monospace,monospace;color:var(--muted)}.agent-run{margin-top:12px;border-top:1px solid var(--line);padding-top:12px}.agent-run-head{display:flex;justify-content:space-between;align-items:flex-start;gap:10px}.agent-run-actions{display:flex;gap:6px;flex-wrap:wrap}.agent-run-actions .btn{padding:7px 9px;font-size:11px}.agent-task{display:grid;grid-template-columns:28px 1fr auto;gap:8px;align-items:start;padding:8px 0;border-top:1px solid #242b32}.agent-task:first-child{border-top:0}.agent-task small{display:block;color:var(--muted);margin-top:3px}.agent-task-state{font-size:10px;border:1px solid var(--line);border-radius:999px;padding:4px 7px}.agent-task-state.CLAIMED{color:#ffc66f}.agent-task-state.COMPLETE{color:var(--lime)}.agent-task-state.FAILED,.agent-task-state.BLOCKED{color:var(--danger)}@media(max-width:700px){.agent-command-row{grid-template-columns:1fr}.agent-result-head,.agent-run-head{display:block}.agent-step,.agent-task{grid-template-columns:24px 1fr}.agent-step span:last-child,.agent-task-state{grid-column:2}}\n',
)
replace_once(
    "desktop/index.html",
    "let productionAgentState={goal:'',result:null,loading:false,error:null,checkpoint:null};\n",
    "let productionAgentState={goal:'',result:null,loading:false,error:null,checkpoint:null,run:null,runLoading:false};\n",
)
replace_once(
    "desktop/index.html",
    '''  return `<section class="agent-console"><div class="agent-command-row"><input id="agentGoal" value="${esc(a.goal)}" placeholder="예: 이 씬 영상 생성 준비 완료까지 진행해줘" ${a.loading?'disabled':''}><button class="btn primary" id="agentRun" ${a.loading?'disabled':''}>${a.loading?'상태 분석 중…':'Agent 실행'}</button></div><div class="agent-quick"><button data-agent-goal="영상 생성 준비 완료까지 진행해줘">Generate-ready</button><button data-agent-goal="업로드 패키지까지 준비해줘">업로드 준비</button><button data-agent-goal="stale 결과를 최신 revision 기준으로 업데이트해줘">Stale 최신화</button></div>${a.error?`<div class="agent-error">${esc(a.error)}</div>`:''}${result}</section>`;\n}\nasync function runProductionAgentGoal(goal){\n''',
    '''  return `<section class="agent-console"><div class="agent-command-row"><input id="agentGoal" value="${esc(a.goal)}" placeholder="예: 이 씬 영상 생성 준비 완료까지 진행해줘" ${a.loading?'disabled':''}><button class="btn primary" id="agentRun" ${a.loading?'disabled':''}>${a.loading?'상태 분석 중…':'Agent 실행'}</button></div><div class="agent-quick"><button data-agent-goal="영상 생성 준비 완료까지 진행해줘">Generate-ready</button><button data-agent-goal="업로드 패키지까지 준비해줘">업로드 준비</button><button data-agent-goal="stale 결과를 최신 revision 기준으로 업데이트해줘">Stale 최신화</button></div>${a.error?`<div class="agent-error">${esc(a.error)}</div>`:''}${result}${productionAgentRunView()}</section>`;\n}\nfunction agentRunLabel(state){return state==='READY'?'실행 가능':state==='PAUSED'?'일시정지':state==='WAITING_REVIEW'?'QA/검토 대기':state==='WAITING_WORK'?'진행 중 작업 대기':state==='BLOCKED'?'차단됨':state==='COMPLETE'?'완료':state==='CANCELLED'?'취소됨':state==='FAILED'?'실패':state||'대기'}\nfunction productionAgentRunView(){const a=productionAgentState,run=a.run;if(!run)return `<div class="agent-run"><div class="agent-run-head"><div><strong>실행 큐</strong><small class="muted">계획을 HAP SQLite에 저장해 앱을 다시 열어도 이어갈 수 있습니다.</small></div><button class="btn" id="agentStartRun" ${a.runLoading?'disabled':''}>${a.runLoading?'생성 중…':'실행 큐 만들기'}</button></div></div>`;const tasks=(run.active_tasks||[]).slice(0,8),failed=(run.tasks||[]).find(x=>x.state==='FAILED'),terminal=['COMPLETE','CANCELLED'].includes(run.state);return `<div class="agent-run"><div class="agent-run-head"><div><strong>실행 큐 · ${esc(agentRunLabel(run.state))}</strong><small class="muted">${esc(run.run_id)} · checkpoint ${esc(String(run.checkpoint||'').slice(0,12))}</small></div><div class="agent-run-actions"><button class="btn" id="agentRefreshRun" ${a.runLoading?'disabled':''}>새로고침</button>${run.state==='PAUSED'?'<button class="btn" id="agentResumeRun">재개</button>':!terminal&&run.state!=='FAILED'?'<button class="btn" id="agentPauseRun">일시정지</button>':''}${failed?`<button class="btn" id="agentRetryRun" data-task="${esc(failed.task_id)}">실패 task 재시도</button>`:''}${!terminal?'<button class="btn danger" id="agentCancelRun">취소</button>':''}</div></div><div class="agent-step-list">${tasks.map(task=>`<div class="agent-task"><span>${task.order}</span><div><b>${esc(task.stage||'작업')}</b><small>${esc(task.instruction||task.suggested_tool||'정본 상태 확인')}</small></div><span class="agent-task-state ${esc(task.state)}">${esc(task.state)}</span></div>`).join('')||`<div class="muted">${run.state==='COMPLETE'?'목표가 정본 기준으로 완료되었습니다.':'현재 활성 task가 없습니다.'}</div>`}</div>${run.last_error?`<div class="agent-error">${esc(run.last_error)}</div>`:''}</div>`}\nasync function startProductionAgentRun(){const goal=String(productionAgentState.goal||productionAgentState.result?.goal||'영상 생성 준비 완료까지 진행해줘').trim();productionAgentState.runLoading=true;renderScene();try{const run=await window.sceneFlow.startProductionRun(selected.project,sceneId(selected),{goal,target:productionAgentState.result?.target});productionAgentState.run=run;productionAgentState.result=run?.production_plan||productionAgentState.result;productionAgentState.checkpoint=run?.checkpoint||productionAgentState.checkpoint;productionAgentState.runLoading=false;renderScene();toast('Production Agent 실행 큐를 만들었습니다')}catch(e){productionAgentState.runLoading=false;productionAgentState.error=e.message||String(e);renderScene();toast('실행 큐 생성 실패: '+(e.message||e))}}\nasync function refreshProductionAgentRun(){if(!productionAgentState.run?.run_id)return;productionAgentState.runLoading=true;renderScene();try{const run=await window.sceneFlow.getProductionRun(selected.project,sceneId(selected),productionAgentState.run.run_id);productionAgentState.run=run;productionAgentState.result=run?.production_plan||productionAgentState.result;productionAgentState.checkpoint=run?.checkpoint||productionAgentState.checkpoint;productionAgentState.runLoading=false;renderScene()}catch(e){productionAgentState.runLoading=false;productionAgentState.error=e.message||String(e);renderScene();toast('실행 큐 새로고침 실패: '+(e.message||e))}}\nasync function controlProductionAgentRun(control,taskId){if(!productionAgentState.run?.run_id)return;productionAgentState.runLoading=true;renderScene();try{const run=await window.sceneFlow.controlProductionRun(selected.project,sceneId(selected),{run_id:productionAgentState.run.run_id,control,task_id:taskId});productionAgentState.run=run;productionAgentState.result=run?.production_plan||productionAgentState.result;productionAgentState.checkpoint=run?.checkpoint||productionAgentState.checkpoint;productionAgentState.runLoading=false;renderScene()}catch(e){productionAgentState.runLoading=false;productionAgentState.error=e.message||String(e);renderScene();toast('실행 큐 제어 실패: '+(e.message||e))}}\nasync function loadLatestProductionAgentRun(){try{const run=await window.sceneFlow.getProductionRun(selected.project,sceneId(selected),null);if(!selected)return;productionAgentState.run=run;productionAgentState.result=run?.production_plan||productionAgentState.result;productionAgentState.checkpoint=run?.checkpoint||productionAgentState.checkpoint;renderScene()}catch(e){/* no previous run is a valid state */}}\nasync function runProductionAgentGoal(goal){\n''',
)
replace_once(
    "desktop/index.html",
    '''  document.querySelectorAll('[data-agent-goal]').forEach(button=>button.onclick=()=>{productionAgentState.goal=button.dataset.agentGoal;runProductionAgentGoal(button.dataset.agentGoal)});\n}\n''',
    '''  document.querySelectorAll('[data-agent-goal]').forEach(button=>button.onclick=()=>{productionAgentState.goal=button.dataset.agentGoal;runProductionAgentGoal(button.dataset.agentGoal)});\n  if($('agentStartRun'))$('agentStartRun').onclick=startProductionAgentRun;if($('agentRefreshRun'))$('agentRefreshRun').onclick=refreshProductionAgentRun;if($('agentPauseRun'))$('agentPauseRun').onclick=()=>controlProductionAgentRun('pause');if($('agentResumeRun'))$('agentResumeRun').onclick=()=>controlProductionAgentRun('resume');if($('agentCancelRun'))$('agentCancelRun').onclick=()=>controlProductionAgentRun('cancel');if($('agentRetryRun'))$('agentRetryRun').onclick=e=>controlProductionAgentRun('retry_task',e.currentTarget.dataset.task);\n}\n''',
)
replace_once(
    "desktop/index.html",
    "productionAgentState={goal:'',result:null,loading:false,error:null,checkpoint:null};render()",
    "productionAgentState={goal:'',result:null,loading:false,error:null,checkpoint:null,run:null,runLoading:false};render();loadLatestProductionAgentRun()",
)

print("reinforcement v8 applied")
